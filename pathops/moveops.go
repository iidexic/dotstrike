package pops

import (
	"os"
)

func ProcessCopy(filelist []string, destDir string) error {
	for i, file := range filelist {
		fsize := os.NewFile
		cr := make(chan []byte, 0)
		_, _, _, _ = fsize, cr, file, i

	}
	return nil
}
